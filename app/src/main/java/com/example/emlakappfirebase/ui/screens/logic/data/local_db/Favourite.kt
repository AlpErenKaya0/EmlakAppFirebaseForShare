/*
package com.example.emlakappfirebase.ui.screens.logic.data.local_db

import androidx.room.Entity
import androidx.room.PrimaryKey


@Entity(tableName = "favorites")
data class FavouriteData(
    @PrimaryKey(autoGenerate = true) val id: Int = 0,
    val userId: String,
    val title: String,
    val name: String,
    val address: String,
    val photos: List<String>,
    val positiveTitle: String,
    val negativeTitle: String,
    val price: String,
    val isPrivate: Boolean
)
buradaki durumları Commitleyeceğim  Yorum Satırına alarak. Şunu görmüş olduk= Room'a resim kaydedilemiyor. */